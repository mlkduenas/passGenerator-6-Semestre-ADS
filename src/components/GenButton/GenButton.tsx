import React from "react";
import { View, Button } from 'react-native'
import { styles } from './GenButton'

export function GenButton(){
    return(
        <Button
            title="Clique aqui!"
        />
    )
}